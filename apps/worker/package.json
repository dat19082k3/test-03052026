{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "predev": "npm run build -w @repo/db",
    "dev": "NODE_ENV=development ts-node-dev --respawn --transpile-only src/index.ts",
    "prebuild": "npm run build -w @repo/db",
    "build": "tsc -p tsconfig.build.json",
    "start": "NODE_ENV=production node dist/index.js",
    "lint": "eslint ."
  },
  "dependencies": {
    "@repo/db": "*",
    "bullmq": "^5.65.1",
    "date-fns": "^4.1.0",
    "dotenv": "^17.4.2",
    "exceljs": "^4.4.0",
    "ioredis": "^5.8.2",
    "pg": "^8.20.0",
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3"
  },
  "devDependencies": {
    "@repo/eslint-config": "^0.0.0",
    "@repo/typescript-config": "^0.0.0",
    "@types/node": "^25.6.0",
    "@types/pg": "^8.20.0",
    "eslint": "^10.2.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^6.0.3"
  }
}
